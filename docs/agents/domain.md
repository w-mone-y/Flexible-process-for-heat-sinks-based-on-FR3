# Domain docs

本仓库使用单上下文领域文档。

## Required reading

在设计、实现或评审前：

1. 阅读根目录 `CONTEXT.md`。
2. 阅读 `docs/adr/` 中与当前区域相关的决策。
3. 在 Issue、测试、代码和文档中使用 `CONTEXT.md` 定义的术语。
4. 如果实现与现有 ADR 冲突，必须显式指出并先更新决策，不能静默覆盖。

领域术语或不可逆决策发生变化时，分别更新 `CONTEXT.md` 或新增 ADR。
