# V2 实施顺序 6～10 完成基线

日期：2026-08-02  
基线起点：`91244c1`

## 已完成能力

6. V2 调度任务在启动前经过 PRM/A*（RRT-Connect 兜底）和时空预约，`/motion/reservations`
   提供实际规划与预约状态。
7. **已按用户后续要求撤销。** V2 不再演示实体换型龙门；梳齿规格仍由订单配置决定，
   到达翅片安装阶段时直接切换显示，沿用此前的阶段驱动逻辑。
8. 故障目录固定为 13 类：7 类自主恢复、6 类人工处置。每类公开检测点、恢复路线、
   最终处置；人工类统一使用非阻塞 10 秒审核状态机。
9. 四组黄金实验已输出 JSON、CSV、JSONL 和 Markdown，并接入 README、Qt 指标页和比赛报告。
10. `physical_execution_complete` 由 11 个强物理门集中计算，任何门失败都会返回失败键和
    中文原因，不再由“逻辑完成 + 无活动 operation”宽松推断。

## 实测验收

- 全量测试：`611 tests`，全部通过。
- Ruff：通过。
- Black：通过。
- Python `compileall`：通过。
- V1 MJCF：`nbody=175, ngeom=898, nu=45`，编译通过。
- V2 MJCF：`nbody=113, ngeom=887, nu=41`，共享换型龙门撤销后编译通过。
- V2 A/B/C 完整物理运行：`170.95 s`，三个订单全部完成，失败/阻塞任务为 0，
  11 个物理完成门全部通过。

## 黄金实验的重要负结果

在 A/B/C 对照中，Arm1 + Arm3 双安装方案为 `72.75 s`，仅 Arm1 方案为 `71.45 s`。
双安装方案慢 `1.82%`，原因是 Arm3 同时承担检测与安装。该结果被保留并用于指导下一阶段：
优先优化检测/安装时窗或增加固定检测资源，而不是把增加机械臂直接等同于提升吞吐。
