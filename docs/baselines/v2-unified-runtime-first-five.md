# V2 统一运行时：前五项实施验收

- 验收日期：2026-08-02
- V1 冻结基线：`91244c1`
- V2 执行模式：`UNIFIED_PHYSICAL_RUNTIME`
- 逻辑权威：`ManufacturingRuntime`
- 物理 actor：`DualLineRuntime` + `DualLineSceneAdapter`

## 已完成范围

1. 冻结 V1，保留入口、A/B/C、三层炉批、故障、暂停/继续/reset 与倍速回归。
2. 建立非阻塞物理技能契约；每次派工使用独立技能实例，任务成功必须携带
   MuJoCo 完成证据。
3. 打通单 A 的任务 DAG、资源授权、V2 actor 和物理反馈闭环。
4. 扩展到 A/B/C/D 与通过规划校验的自定义订单。
5. 打通多订单全局动态调度、Arm1/Arm3 AND/OR 安装资源选择、共享合流区和
   三层兼容炉批。

## 代表性物理验收

| 场景 | 仿真完成时间 | 结果 |
|---|---:|---|
| A/B/C 三个并发订单 | `170.05 s` | 全部完成，严格物理完成为真，双支路重叠安装 20.0 s |
| A + D 混合订单 | `134.5 s` | A 5/5、D 9/9 翅片完成，两安装支路均使用 |
| 自定义 6 翅片订单 | `104.5 s` | 6/6 翅片完成，失败任务为 0 |

物理任务的 `TASK_SUCCEEDED` 事件记录 `physical_completion`，包含观测时间、
反馈来源和已通过的完成条件。预计时长只参与调度、进度和超时，不再作为成功
判据。

## 回归结果

```text
pytest -q
605/605 passed
```

同时通过：

- Ruff 全项目检查。
- Black 本轮 Python 文件检查。
- Python `compileall`。
- V1/V2 MJCF 编译。
- 完整载荷的 B 支路 Arm3 净空、关节连续性与 V1 姿态锁定语义回归。

测试仅产生 2 条既有 NumPy 标量转换弃用警告，不影响当前结果。
