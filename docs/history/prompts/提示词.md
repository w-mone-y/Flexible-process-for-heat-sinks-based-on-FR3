~~~markdown
你现在是一名精通 MuJoCo、MJCF、Python、机器人运动控制、柔性工装和自动化产线仿真的高级工程师。

请在我现有的 MuJoCo 三机械臂生产线项目中，设计并实现一套“散热基板—翅片炉中钎焊装配系统”。

请先完整检查当前项目目录、主 Python 文件、主 XML 文件、已有机器人名称、Table1/Table2/Table3、Arm1/Arm2/Arm3、工具切换系统和 equality weld 结构，再进行修改。

不要盲目重写整个项目，不要删除现有功能。应尽量采用模块化新增和局部修改。

最终必须给出完整、可以运行的代码，而不是只给示例片段。

---

# 一、项目工艺目标

本项目生产对象为：

```text
散热基板
+
多片平行竖直薄板翅片
+
预涂钎焊材料
+
可重复使用的钎焊工装
~~~

整个 Table2 工艺顺序统一为：

```text
1. Arm1 将散热基板放入工装托盘
2. 工装根据当前订单更换或启用对应梳齿模块
3. Arm2 在所有翅片槽位两侧预先布置钎焊材料
4. Arm3 检查钎焊材料路径
5. Arm1 依次安装所有翅片
6. Arm3 检查翅片数量、位置、间距和垂直度
7. 安装或下降上压板
8. 上压板缓慢接触翅片并逐渐施加压力
9. 达到目标夹紧力后保持
10. 激活逻辑锁紧约束
11. 整套工装进入 READY_FOR_TRANSFER 状态
12. 后续由 Arm2 或炉前滑台将工装送入钎焊炉
```

必须坚持以下工艺原则：

> 无论翅片间距大还是小，都采用“先在基板上预涂钎焊材料，再安装翅片”的统一流程。

不允许在翅片全部安装后，再让喷嘴进入狭窄翅片间隙涂覆。

------

# 二、统一坐标约定

请在所有 XML、Python、路径生成和注释中使用统一坐标系：

```text
X轴：翅片长度方向
Y轴：不同翅片排列和间距方向
Z轴：竖直向上
```

散热基板位于 XOY 平面。

每片翅片：

```text
长度沿 X 方向
厚度沿 Y 方向
高度沿 Z 方向
```

简图：

```text
                 Z
                 ↑
                 │     翅片
                 │       │
                 │       │
                 └────────────→ X
                /
               /
              Y
```

------

# 三、工装整体结构

请在 Table2 上增加一个可整体搬运的钎焊工装。

推荐层级：

```text
fixture_tray
├── fixture_tray_geom
├── fixture_lower_plate
├── base_plate_target_site
├── base_locator_left
├── base_locator_right
├── base_locator_front
├── base_locator_rear
│
├── fixture_front_comb_frame
│   ├── front_comb_left_support
│   ├── front_comb_right_support
│   └── fixture_front_comb_insert
│
├── fixture_rear_comb_frame
│   ├── rear_comb_left_support
│   ├── rear_comb_right_support
│   └── fixture_rear_comb_insert
│
├── fixture_upper_press_system
│   ├── fixture_press_drive
│   ├── fixture_press_floating_joint
│   ├── fixture_upper_plate
│   ├── fixture_front_press_bar
│   └── fixture_rear_press_bar
│
├── fixture_tray_left_handle
├── fixture_tray_right_handle
└── fixture_tray_grasp_site
```

只有 `fixture_tray` 的底面与 Table2 接触。

以下部件均应安装在托盘上，并随托盘整体移动：

```text
散热基板
前后梳齿支架
梳齿模块
上压板机构
基板定位块
所有翅片
```

不要把梳齿板直接固定在 Table2 桌面上。

------

# 四、散热基板

散热基板命名为：

```text
heatsink_base_plate
```

第一版推荐尺寸，必须放入配置区，不允许散落在代码中：

```text
基板长度 X：0.36 m
基板宽度 Y：0.22 m
基板厚度 Z：0.008 m
```

第一版可以使用完整 `box geom`，不强制做真实布尔凹槽。

使用目标 site 表示每片翅片的理论安装中心：

```text
fin_slot_01_target
fin_slot_02_target
fin_slot_03_target
...
```

钎焊材料路径同样根据这些槽位自动生成。

------

# 五、悬空式前后梳齿板

## 5.1 安装位置

梳齿板不与散热基板接触。

前后梳齿板通过左右支撑立柱安装在 `fixture_tray` 上，并悬空在散热基板上方。

正确结构：

```text
                   梳齿模块
          ┌─┬──┬──┬──┬─┐
左支撑柱  │ │  │  │  │ │  右支撑柱
    │     └─┴──┴──┴──┴─┘      │
    │         │  │  │          │
    │         │  │  │ 翅片     │
    │         │  │  │          │
══════════════╧══╧══╧══════════════
              散热基板

───────────────────────────────────
              工装托盘
```

梳齿板受力路径：

```text
梳齿模块
→ 支撑框架
→ 工装托盘
```

不能设计成：

```text
梳齿模块
→ 散热基板
```

## 5.2 前后布置

在翅片长度方向设置两套梳齿板：

```text
翅片前端内部位置：fixture_front_comb
翅片后端内部位置：fixture_rear_comb
```

俯视简图：

```text
X方向 →

┌──────────────────── 散热基板 ────────────────────┐
│                                                  │
│ 前梳齿                                          后梳齿
│ ┌┬┬┬┬┐======================================┌┬┬┬┬┐
│ ││││││======================================││││││
│ ││││││======================================││││││
│ └┴┴┴┴┘======================================└┴┴┴┴┘
│                                                  │
└──────────────────────────────────────────────────┘
```

前后梳齿槽必须一一对齐：

```text
front_slot_01_y == rear_slot_01_y
front_slot_02_y == rear_slot_02_y
...
```

否则翅片会发生扭斜。

## 5.3 梳齿定位高度

梳齿板只卡住翅片中部或中下部，不能遮挡翅片根部钎焊区域。

建议把翅片高度分为：

```text
顶部区域：
供上压板轻压

中部区域：
供前后梳齿模块定位

根部区域：
完全留给钎焊材料和接头
```

截面简图：

```text
             翅片顶部
                │
       ┌────────┼────────┐
       │    梳齿定位区    │
       └────────┼────────┘
                │
                │
                │  ← 根部区域必须开放
════════════════╧════════════════
             散热基板
```

------

# 六、统一框架和可更换梳齿模块

不要针对每种产品更换整套工装。

采用：

> 固定支撑框架＋可更换梳齿模块。

固定部分：

```text
fixture_front_comb_frame
fixture_rear_comb_frame
左右支撑柱
模块定位销
模块锁扣
```

可更换部分：

```text
front_comb_insert_20mm
rear_comb_insert_20mm

front_comb_insert_30mm
rear_comb_insert_30mm

front_comb_insert_40mm
rear_comb_insert_40mm
```

示意：

```text
固定支撑框架
┌────────────────────────────┐
│      可拆卸梳齿模块        │
│       ∪  ∪  ∪  ∪          │
└────────────────────────────┘
```

请至少实现三种产品配置：

```python
PRODUCT_A:
    fin_count = 5
    fin_pitch = 0.020

PRODUCT_B:
    fin_count = 4
    fin_pitch = 0.030

PRODUCT_C:
    fin_count = 3
    fin_pitch = 0.040
```

这些数值只是仿真初值，应集中定义在配置数据类中。

切换产品时：

```text
读取订单
→ 选择对应前梳齿模块
→ 选择对应后梳齿模块
→ 隐藏或禁用其他模块
→ 校验前后模块规格相同
→ 计算所有槽位坐标
→ 更新槽位 target site
→ 状态改为 COMB_CONFIGURED
```

第一版不要求机械臂真实拆换梳齿模块，可以使用以下方式模拟：

```text
显示当前模块；
隐藏非当前模块；
禁用非当前模块碰撞；
更新 active_comb_module；
播放简单换型动画或日志。
```

但代码结构必须为后续“机械臂真实抓取梳齿模块换型”保留接口。

推荐函数：

```python
configure_comb_module(product_config)
activate_comb_insert(module_name)
deactivate_comb_insert(module_name)
validate_front_rear_comb_alignment()
generate_fin_slot_targets(product_config)
```

------

# 七、Arm2 双喷嘴钎焊材料涂覆工具

请为 Arm2 增加一个轻量化叉形双通道涂覆工具。

命名：

```text
arm2_dual_brazing_dispenser_tool
arm2_dispenser_mount
arm2_dispenser_manifold
arm2_left_nozzle
arm2_right_nozzle
arm2_dispenser_center_tcp
arm2_left_nozzle_tip_site
arm2_right_nozzle_tip_site
```

整体结构：

```text
                    Arm2 法兰
                        │
                  工具快换连接板
                        │
                    分流块
                   /      \
             左输料管    右输料管
                │           │
             左喷嘴       右喷嘴
                ↓           ↓
════════════════●═══槽位═══●════════════════
                    散热基板
```

注意：

- 双喷嘴操作时，翅片尚未安装；
- 双喷嘴跨越的是空槽位，而不是已经安装的翅片；
- 两个喷嘴分别在槽中心左右两侧形成钎焊材料线；
- 喷嘴不能正对槽中心，以免后续翅片插入时把全部材料挤走。

喷嘴建议：

```text
向内倾斜约 30°～45°
出口距离基板表面约 1～2 mm
左右对称
左右输料路径长度和阻力保持一致
```

这些数值仅为仿真初始参数，必须放入配置类。

推荐工具截面：

```text
                左喷嘴       右喷嘴
                    \         /
                     ▼       ▼
                    ●   │   ●
                        │
                    翅片槽中心
══════════════════════════════════
                    基板
```

喷嘴间距不要直接写死，应根据以下参数生成：

```text
翅片厚度
焊料线相对槽中心的偏置
喷嘴安全间隙
```

推荐配置：

```python
@dataclass
class DispenserConfig:
    nozzle_spacing: float
    bead_offset_from_slot_center: float
    tip_height: float
    approach_height: float
    travel_speed: float
    nozzle_inward_angle_deg: float
    anti_drip_enabled: bool
```

------

# 八、钎焊材料路径生成

每个翅片槽位对应两条路径：

```text
slot_01_left_brazing_path
slot_01_right_brazing_path

slot_02_left_brazing_path
slot_02_right_brazing_path
...
```

设第 `i` 个槽中心坐标为：

```text
y_i
```

则：

```text
左侧材料线：y_i - bead_offset
右侧材料线：y_i + bead_offset
```

每条路径沿 X 方向。

必须根据订单自动生成，不允许人工逐条硬编码。

推荐数据结构：

```python
@dataclass
class BrazingPath:
    path_id: str
    slot_id: str
    side: str
    start_pos: tuple[float, float, float]
    end_pos: tuple[float, float, float]
    target_width: float
    target_coverage: float
    progress: float = 0.0
    status: str = "pending"
```

推荐函数：

```python
generate_all_brazing_paths(product_config)
apply_brazing_path(path)
validate_brazing_paths()
reset_brazing_material()
```

------

# 九、涂覆动作顺序

所有翅片安装之前，Arm2 一次性完成全部槽位的涂覆。

动作顺序：

```text
Arm2 获取双喷嘴工具
→ 移动到 slot_01 起点上方
→ 下降到涂覆高度
→ 左右喷嘴同时打开
→ 沿 X 方向匀速运动
→ 左右同时形成材料线
→ 到达终点前关闭出料
→ 防滴漏回吸
→ 抬升
→ 移动到 slot_02
→ 重复
→ 所有槽位完成
→ Arm2 撤离 Table2
```

不要使用：

```python
time.sleep()
```

必须使用非阻塞状态机，每个仿真 step 更新一次。

推荐状态：

```text
DISPENSER_IDLE
→ MOVE_TO_PATH_APPROACH
→ DESCEND_TO_PATH
→ START_DISPENSING
→ FOLLOW_PATH
→ STOP_DISPENSING
→ RETRACT
→ MOVE_TO_NEXT_PATH
→ DISPENSING_COMPLETE
```

------

# 十、钎焊材料视觉表现

MuJoCo 第一版不模拟真实流体。

请采用“分段细长 geom”表现材料线逐渐生成。

例如：

```text
slot_01_left_bead_segment_001
slot_01_left_bead_segment_002
...
slot_01_right_bead_segment_001
...
```

初始隐藏：

```text
alpha = 0
```

喷嘴经过对应区域后：

```text
alpha = 1
```

或者通过缩放长度逐渐显示。

必须实现：

```text
材料线随 Arm2 运动逐渐出现
左右材料线同步生成
可以单独标记漏涂或断线
可以重置全部材料线
```

建议颜色与散热基板形成明显对比，但不要使用过度鲜艳的视觉效果。

------

# 十一、Arm3 钎焊材料检测

Arm2完成全部槽位涂覆后，Arm3进行检测。

第一版可使用逻辑检测，不必实现真实视觉算法。

检测内容：

```text
所有订单要求的路径是否存在
每条路径覆盖率是否达到阈值
是否存在漏涂
是否存在断续
左右路径是否与槽中心保持正确偏置
是否有路径超出基板
```

结果：

```text
MATERIAL_PASSED
MATERIAL_REWORK_REQUIRED
```

如有缺陷，Arm2只补涂缺陷路径或缺陷区间。

------

# 十二、Arm1 安装翅片

钎焊材料检测通过后，Arm1依次安装翅片。

顺序：

```text
抓取 fin_01
→ 移动到 slot_01 上方
→ 翅片保持竖直
→ 同时对准前后梳齿槽
→ 缓慢下降
→ 根部到达基板目标高度
→ 释放
→ 逻辑预夹紧
→ 安装下一片
```

前后梳齿应允许翅片从上方插入。

槽口建议具有导向斜面或较宽入口：

```text
       翅片
         ↓
       \   /
        \ /
        │ │
        │ │
```

槽宽和翅片厚度必须分开配置。

例如：

```text
fin_thickness = 0.002
comb_slot_width = 0.0025
```

MuJoCo 为提高稳定性，可以适当增加仿真间隙，但代码注释必须说明这不是实际制造公差。

安装后状态：

```text
FIN_01_INSTALLED
FIN_02_INSTALLED
...
ALL_FINS_INSTALLED
```

------

# 十三、上压板和上下压紧

所有翅片安装并检测通过后，执行上压板压紧。

上压板不应使用整块刚性平板直接压住薄翅片中间。

建议上压板底部设置两条横向压条：

```text
翅片前部压条
翅片后部压条
```

俯视：

```text
          前压条
────────────────────────

│  │  │  │  │  │  │  │
│  │  │  │  │  │  │  │

────────────────────────
          后压条
```

压条应靠近翅片长度方向的前后区域，避开主要钎焊根部路径。

实现方式：

```text
位置执行器控制压板下降
+
浮动弹簧关节吸收位置误差
+
touch sensor 检测接触力
+
达到目标力后逻辑锁紧
```

状态机：

```text
PRESS_OPEN
→ PRESS_CONTACT_SEARCH
→ PRESS_FORCE_RAMP
→ PRESS_FORCE_HOLD
→ FIXTURE_LOCKED
```

禁止直接把上压板瞬间移动到最低点。

必须缓慢寻找接触。

推荐 XML 结构：

```text
fixture_press_drive
└── fixture_press_drive_slide
    └── fixture_press_floating_body
        ├── fixture_press_floating_joint
        ├── fixture_upper_plate
        ├── fixture_front_press_bar
        └── fixture_rear_press_bar
```

使用：

```text
position actuator
forcerange
touch sensor
jointpos sensor
actuatorfrc sensor
```

达到目标夹紧力并稳定保持后，再激活：

```text
base_plate_to_fixture_weld
fin_01_to_fixture_weld
fin_02_to_fixture_weld
...
```

接触力负责展示真实压紧过程。

`equality weld`负责后续搬运过程中的稳定性。

------

# 十四、完整状态机

请实现一个非阻塞的 Table2 工艺状态机：

```text
IDLE

→ LOAD_BASE_PLATE
→ BASE_PLATE_POSITIONED

→ CONFIGURE_COMB_MODULE
→ COMB_CONFIGURED

→ GET_DISPENSER_TOOL
→ GENERATE_BRAZING_PATHS
→ APPLY_ALL_BRAZING_MATERIAL
→ MATERIAL_INSPECTION

→ MATERIAL_REWORK
→ MATERIAL_PASSED

→ RETURN_DISPENSER_TOOL
→ INSTALL_FINS
→ FIN_GEOMETRY_INSPECTION

→ FIN_REWORK
→ FINS_PASSED

→ INSTALL_OR_ENABLE_UPPER_PLATE
→ PRESS_CONTACT_SEARCH
→ PRESS_FORCE_RAMP
→ PRESS_FORCE_HOLD

→ ACTIVATE_FIXTURE_LOCKS
→ READY_FOR_TRANSFER

→ ERROR
```

每个状态必须包含：

```text
进入条件
执行动作
完成条件
超时条件
错误恢复动作
日志输出
```

同一时刻只允许一台机械臂进入 Table2 核心区域。

增加资源锁：

```python
table2_zone_owner = None
```

可能的值：

```text
None
ARM1
ARM2
ARM3
FIXTURE
```

必须防止 Arm1、Arm2、Arm3 同时进入 Table2 核心工作区。

------

# 十五、推荐配置结构

请将参数集中管理，不要在各函数中散落魔法数字。

```python
@dataclass
class ProductConfig:
    product_id: str
    fin_count: int
    fin_pitch: float
    fin_length: float
    fin_height: float
    fin_thickness: float
    base_length: float
    base_width: float
    base_thickness: float
    comb_module_name: str


@dataclass
class FixtureConfig:
    tray_size: tuple[float, float, float]
    front_comb_x: float
    rear_comb_x: float
    comb_height: float
    comb_slot_width: float
    base_clearance: float
    target_clamping_force: float
    clamping_force_tolerance: float
    force_hold_duration: float


@dataclass
class DispenserConfig:
    nozzle_spacing: float
    bead_offset_from_slot_center: float
    nozzle_tip_height: float
    approach_height: float
    travel_speed: float
    nozzle_inward_angle_deg: float
    target_bead_width: float
    anti_drip_enabled: bool
```

------

# 十六、建议对象名称

必须统一使用以下命名风格，不要生成无意义的随机名称。

## 工装

```text
fixture_tray
fixture_lower_plate
fixture_base_target
fixture_front_comb_frame
fixture_rear_comb_frame
fixture_front_comb_insert
fixture_rear_comb_insert
fixture_upper_plate
fixture_front_press_bar
fixture_rear_press_bar
fixture_press_slide
fixture_press_floating_joint
fixture_tray_grasp_site
```

## 产品

```text
heatsink_base_plate
fin_01
fin_02
fin_03
...
fin_slot_01_target
fin_slot_02_target
...
```

## 钎焊材料

```text
slot_01_left_brazing_path
slot_01_right_brazing_path
slot_01_left_bead_segment_001
slot_01_right_bead_segment_001
...
```

## Arm2 工具

```text
arm2_dual_brazing_dispenser_tool
arm2_dispenser_center_tcp
arm2_left_nozzle_tip_site
arm2_right_nozzle_tip_site
```

## 约束

```text
arm2_dispenser_tool_weld
base_plate_to_fixture_weld
fin_01_to_fixture_weld
fin_02_to_fixture_weld
...
```

------

# 十七、MuJoCo 实现约束

请遵守以下要求：

1. 不模拟真实钎焊冶金和真实流体；
2. 使用几何动画、状态变量和逻辑质量参数表示钎焊材料；
3. 不使用大量 `time.sleep()`；
4. 所有流程使用非阻塞状态机；
5. 不要在正常运动过程中直接瞬移动态物体；
6. 只允许在初始化、订单切换和模块切换时进行受控状态重置；
7. 抓取和最终锁紧可使用 equality weld；
8. 上压板接触过程必须使用真实 slide joint 和 actuator；
9. 所有 name 查找必须检查 `mj_name2id` 返回值；
10. 缺失对象时输出清晰中文错误；
11. 不允许静默忽略 XML 或对象名称错误；
12. 保留现有机器人、相机、灯光、UI和已有功能；
13. XML 拆分时使用清晰的 include 层级；
14. 必须保证 XML 可被 MuJoCo 成功加载；
15. 必须保证 Python 无语法错误并可运行。

------

# 十八、建议文件组织

如果当前项目适合拆分，请采用类似结构：

```text
project/
├── main.py
├── scene.xml
├── models/
│   ├── robots.xml
│   ├── tables.xml
│   ├── brazing_fixture.xml
│   ├── brazing_dispenser_tool.xml
│   ├── heatsink_products.xml
│   └── furnace.xml
│
├── controllers/
│   ├── table2_process_controller.py
│   ├── fixture_press_controller.py
│   ├── dispenser_controller.py
│   ├── comb_module_controller.py
│   └── resource_lock_controller.py
│
└── config/
    └── product_configs.py
```

如果当前项目是单文件，也可以先保留单文件，但必须用类和清晰区域划分，不能把所有逻辑堆进主循环。

------

# 十九、必须实现的预检

程序启动时执行 `preflight_check()`。

至少检查：

```text
fixture_tray 是否存在
base_plate_target_site 是否存在
前后梳齿框架是否存在
当前梳齿模块是否匹配
Arm2 双喷嘴工具是否存在
左右喷嘴 tip site 是否存在
上压板 slide joint 是否存在
压紧 actuator 是否存在
touch sensor 是否存在
所有订单槽位是否在基板范围内
前后梳齿槽是否一一对齐
双喷嘴材料线是否超出基板
所有必要 equality weld 是否存在
```

如果失败，必须停止流程并输出：

```text
缺失对象名称
对象类型
所在功能模块
建议检查的 XML 文件
```

------

# 二十、验收标准

最终代码必须满足以下验收条件。

## 工装结构

- 梳齿板安装在托盘支撑框架上；
- 梳齿板悬空在基板上方；
- 梳齿板不直接接触 Table2；
- 梳齿板不依赖基板支撑；
- 整套工装可随托盘整体移动。

## 模块换型

- 可以在至少三种产品配置之间切换；
- 前后梳齿模块始终配套；
- 槽位数量和间距随订单变化；
- 非当前模块隐藏并禁用碰撞。

## 涂覆过程

- 所有翅片安装前完成钎焊材料预涂；
- 双喷嘴同时形成槽位左右两条材料线；
- 材料线沿 X 方向；
- 不需要进入已安装翅片之间；
- 材料线能够逐段显示。

## 翅片装配

- Arm1按照槽位顺序安装翅片；
- 翅片前后端同时进入对应梳齿槽；
- 翅片根部到达基板表面；
- 安装后翅片不会明显倾倒。

## 压紧

- 上压板缓慢下降；
- 能检测首次接触；
- 能逐渐达到目标夹紧力；
- 能保持目标力；
- 达标后激活逻辑锁紧；
- 搬运时基板和翅片不会散落。

## 软件质量

- 无明显魔法数字；
- 无阻塞式流程；
- 有完整中文日志；
- 有错误恢复；
- XML和Python能直接运行；
- 不破坏现有项目功能。

------

# 二十一、输出要求

请严格按以下顺序工作并输出结果：

1. 列出你检查到的当前项目文件和关键对象；
2. 说明你准备修改和新增哪些文件；
3. 给出工装、梳齿模块和双喷嘴的结构设计；
4. 给出完整状态机；
5. 实际修改代码；
6. 对 XML 进行加载验证；
7. 对 Python 进行语法检查；
8. 输出完整修改后的文件内容或直接修改项目文件；
9. 给出运行命令；
10. 给出测试步骤；
11. 列出仍需我确认的真实工艺参数。

不要只输出伪代码。

不要只给零散函数。

不要让我自己把多个代码片段拼起来。

如果发现现有项目的坐标系、对象名称或机器人结构与本提示词假设不同，应以当前项目实际结构为准进行适配，但必须保留本提示词规定的工艺逻辑。

最终目标是：

> 在 MuJoCo 中完整展示“工装换型—基板定位—全部槽位双侧预涂钎焊材料—翅片依次安装—上压板施压—工装锁紧—准备转运进炉”的柔性生产过程。

```

```