# FR3 柔性钎焊产线领域上下文

## 系统目标

在 MuJoCo 中展示三台 FR3 面向动态订单的取放、钎料涂覆、检测、
翅片安装、批次钎焊和成品交付，并以安全的动态调度提升多订单吞吐量。

## 统一术语

- **能力（Capability）**：工序类型的工厂级契约，声明工具类别、参数范围、
  参数化节拍、工艺谓词，定义在 `config/capabilities.yaml`。
- **工艺路线（Routing）**：某产品要经过哪些工序及其依赖，定义在
  `config/routings/`；用 `$name` 引用计划变量，因此与具体产品无关。
- **OR 分支（Alternative）**：同一工序的等价替代路线，必须产生相同工艺效果。
- **延迟绑定**：任务只声明所需能力，具体资源在构图/派工时刻由能力、工具类别、
  参数窗口和产线剖面共同算出。
- **产线执行剖面**：某条产线的 actor 实际能驱动哪些资源。能力数据描述机器人，
  剖面描述场景；V1 的翅片技能只实现在 Arm1 上，故 V1 剖面不把 Arm3 列为候选。
- **V1**：现有浅 U 形单安装支路产线，由 `brazing_line.py` 启动。
- **V2 / 双安装线**：独立的新产线入口；焊料检测后分流到两条翅片安装支路。
- **产品单元**：一个订单中的一件散热器，始终绑定一个托盘。
- **托盘**：承载模具、基板、翅片、梳齿和压片的唯一物流载体。
- **安装线 A**：由 Arm1 服务的专职翅片安装支路。
- **安装线 B**：由 Arm3 复合末端服务的兼职翅片安装支路。
- **共享焊前检测**：两条安装支路合流后，由 Arm3 执行的入炉前检测。
- **平面绕障物流**：S3A 托盘从 S2B 北侧、S3B 托盘从 Arm3 单元南侧
  沿连续双滑轨绕行至 S4；载有产品的托盘全程保持 Z=0.225 m。
- **炉后检测**：炉体后门与成品出口之间的固定视觉门架检测。
- **贯通式批次炉**：前门装载、三层批次钎焊、后门卸载的 CAB 炉。
- **炉批**：一至三件工艺兼容产品共享的一次不可拆分炉周期。
- **虚拟返回**：成品移除后，空托盘只在状态机中返回上料缓冲区，不显示返回输送线。
- **托盘所有权**：任一时刻托盘只能由一个工位、输送机构、炉层或缓冲区拥有。
- **物理任务**：MuJoCo actor 已真实完成的任务；任务图状态不能提前代表物理完成。
- **数字孪生快照**：在决策边界从现有运行时状态复制出的不可变事实集合，包含仿真时间、计划版本和状态指纹；不拥有订单或任务状态。
- **影子时长估计**：只消费真实任务开始/完成事件的预测统计；当前只用于观测和实验，不覆盖任务名义节拍。
- **决策事件**：描述计划提议、提交、安全检查或估计更新的可序列化事件；不是任务完成事件的替代物。
- **参考计划**：CP-SAT 对某一数字孪生快照中的活动任务，在给定时限内生成的最优或有界可行计划；只用于比较，不直接派工。
- **已承诺任务**：已经处于 `RESERVED` 或 `RUNNING` 的任务；参照计划必须固定其资源并从当前相对时刻 0 继续，不能抢占或改派。
- **最优性差距**：可行解目标值相对 CP-SAT 最佳下界的归一化差值；只有 gap 为 0 且状态为 `OPTIMAL` 才能宣称该模型实例已证明最优。
- **承诺窗口**：在线重规划后允许原子提交、且本轮不再改变的安全任务前缀；V2 Phase 4
  只提交下一组最多三个无资源/工位/区域冲突的 READY 任务。
- **权威派工**：TwinShield 候选通过快照新鲜度和实时可执行门控后，替代当前调度器选择
  下一承诺窗口；不替代物理 actor 或完成证据。
- **确定性回退**：TwinShield 求解、校验或原子提交失败时，使用同一 tick 已计算的
  `DynamicPriorityScheduler` 候选继续派工，不把订单置为 ERROR。
- **安全屏障**：计划提交前检查几何碰撞、净空、时空预约和设备互锁的最终门控；必须先经影子模式验证再逐段强制。
- **潜伏物理缺陷**：生产工序已经在指定焊缝/翅片上形成、但尚未被相机确认的缺陷；
  只生成 `PhysicalFaultState`，不得提前生成恢复计划。
- **质量返工回路**：相机检出后，托盘按唯一所有权实体返回 S2A 或原安装支路，
  修复后必须再次通过对应检测才能恢复主流程。

## 不变量

- 数据驱动构图必须与重构前手写 DAG 等价：A/B/C × 1/3 件 × V1/V2 拓扑下
  节点集、依赖边、区域、节拍与资源绑定完全一致。
- 能力绑定得不到候选时保留原有绑定并告警，禁止产出无人可执行的任务。
- 产线剖面禁止把任务派给该产线 actor 无法驱动的资源。
- 工艺路线只描述产品工艺；工位分配、托盘移载与合流互斥属产线拓扑。
- 节拍表达式只允许算术运算，禁止 `eval` 与任意调用。
- 柔性化只增加配置能力，不放松物理校验（如梳齿槽数、对象池上限）。
- V1 与 V2 可独立启动，V2 实现不能破坏 V1。
- 产品状态绑定托盘，不绑定固定工位。
- 除明确批准的空托盘虚拟返回外，载有产品的托盘禁止瞬移。
- 质量返工的状态回退必须伴随实体托盘所有权与水平运输回退；禁止只改 `UnitStage`。
- Arm3 检测优先，但已经开始的单片翅片安装不可抢占。
- 合流区、共享检测台和炉口都必须先原子预留再移动。
- S3 出站必须以完整托盘载荷包围盒校验 S2B 与 Arm3 净空，禁止只检查托盘底板。
- 已锁入炉批的托盘不能被紧急订单替换。
- 不兼容产品不能混炉。
- 0.25× 至 32× 只改变演示推进速度，不改变任务依赖和质量结果。
- CP-SAT 参考求解不得调用任务派工、推进仿真或修改 MuJoCo actor；快照指纹在求解前后必须保持稳定。
- TwinShield 权威窗口必须整体提交或整体回滚；禁止留下部分 RUNNING 任务、资源/区域预约
  或运输托盘所有权。
- TwinShield 不得由单纯仿真时间推进触发重算，只能由 READY、资源、区域或工位事实变化触发。
