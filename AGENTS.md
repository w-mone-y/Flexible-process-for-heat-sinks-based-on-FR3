## Agent skills

### Issue tracker

项目任务与规格使用当前 GitHub 仓库的 Issues 管理。详见
`docs/agents/issue-tracker.md`。

### Triage labels

使用默认五类任务状态标签。详见 `docs/agents/triage-labels.md`。

### Domain docs

项目采用单上下文领域文档结构：根目录 `CONTEXT.md` 与
`docs/adr/`。详见 `docs/agents/domain.md`。
