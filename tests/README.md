# 测试目录

- `shared/`：不依赖某一场景版本的领域模型测试。
- `v1/`：标准产线、柔性订单、物理 actor、故障恢复和旧入口回归。
- `v2/`：双安装支路、V2 MJCF、场景适配、调度门控和 UI/API 集成。

常用命令：

```bash
make test       # 全部测试
make test-v1    # shared + V1
make test-v2    # V2
```

测试文件可以分类移动，但根入口和公开行为必须保持兼容。涉及场景结构的修改同时运行
对应版本测试和全量 `make test`。
